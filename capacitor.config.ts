import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.necdetuygur.adbf',
  appName: 'Altın Döviz Borsa Fiyatları',
  webDir: 'dist/ng-adf',
  bundledWebRuntime: false,
};

export default config;
